execution completed with status=Ok but program output did NOT match SUCCESS_MARKER='^5$'. Actual recent output lines (comma-joined): 2735 |         let line = ls.linenumber;,2756 |         jf = v.f;,2750 |             jf = 0; // placeholder,2791 |     let fvar = ls.fs.as_ref().unwrap().nactvar as i32;,2798 |     let pc = ls.fs.as_ref().unwrap().pc;,2807 |         let attr_name = str_check_name(ls, state)?;,2821 | fn checktoclose(ls: &mut LexState, state: &mut LuaState, level: i32) -> Result<(), LuaError> {,2825 |         let rl = reg_level(ls, ls.fs.as_ref().unwrap(), level);,2835 |     let mut vidx = 0i32;,2940 |     let first = {,2958 |                     let info = e.u.info as usize;,2946 |         nret = 0;,1016 | fn local_debug_info<'a>(ls: &LexState, fs: &'a mut FuncState, vidx: i32) -> Option<&'a mut LocalVar> {,1174 | fn new_upvalue(,1228 | fn markupval(fs: &mut FuncState, level: i32) {,1257 | fn singlevaraux(,1526 |     drop(fs); // release borrow before iterating,1526 -     drop(fs); // release borrow before iterating,1526 +     let _ = fs; // release borrow before iterating,nil,. Likely causes: (1) codegen emits incomplete bytecode for an expression — e.g. ARITH ops (OP_ADD/OP_ADDI/OP_ADDK) not wired, or (2) a VM dispatch arm uses todo!() that's being masked. Investigate by reading the proto.code for the test program — if missing OP_ADD or similar, the bug is in lua-parse/codegen; if the bytecode is correct but execution gives wrong result, the bug is in lua-vm/vm.rs dispatch.
