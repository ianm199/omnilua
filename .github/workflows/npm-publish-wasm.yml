name: Publish lua-rs-wasm

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Run npm publish --dry-run instead of publishing"
        required: true
        default: true
        type: boolean
      tag:
        description: "npm dist-tag"
        required: true
        default: "latest"
        type: choice
        options:
          - latest
          - next

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: publish lua-rs-wasm
    runs-on: ubuntu-latest
    steps:
      - name: require main branch
        run: |
          if [ "$GITHUB_REF" != "refs/heads/main" ]; then
            echo "This workflow must run from main; got $GITHUB_REF" >&2
            exit 1
          fi

      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"

      - uses: Swatinem/rust-cache@v2

      - name: verify package gate
        run: WASM_SKIP_BROWSER=1 ./harness/check_wasm_package.sh

      - name: npm publish dry-run
        if: ${{ inputs.dry_run }}
        working-directory: packages/lua-rs-wasm
        run: npm publish --dry-run --tag "${{ inputs.tag }}"

      - name: npm identity
        if: ${{ !inputs.dry_run }}
        working-directory: packages/lua-rs-wasm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm whoami

      - name: npm publish
        if: ${{ !inputs.dry_run }}
        working-directory: packages/lua-rs-wasm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --provenance --tag "${{ inputs.tag }}"
